apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grounded-rag
resources:
  - namespace.yaml
  - config.yaml
  - postgres.yaml
  - api.yaml
  - web.yaml
  # ingest-job.yaml is applied on demand, not part of the steady-state apply
